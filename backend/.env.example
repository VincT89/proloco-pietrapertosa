# Configurazione Database MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=proloco_db

# Porta del Server Backend
PORT=4000

# Sicurezza JWT
JWT_SECRET=tuo_segreto_super_sicuro_cambiami

# Cloudinary (per upload media)
CLOUDINARY_CLOUD_NAME=nome_cloud
CLOUDINARY_API_KEY=api_key
CLOUDINARY_API_SECRET=api_secret

# CORS Whitelist (URL consentiti, senza slash finale)
FRONTEND_URL=https://tuodominio.it
ADMIN_URL=https://admin.tuodominio.it
